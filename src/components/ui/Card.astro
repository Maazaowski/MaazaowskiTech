---
import { cn } from '../../lib/utils';

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={cn('bg-white dark:bg-gray-800 rounded-lg shadow-lg overflow-hidden', className)}>
  <slot />
</div>